package com.rightmeprove.airbnb.airBnbApp.entity.enums;

/**
 * Enum representing different user roles in the system.
 */
public enum Role {
    GUEST,          // Regular user who can search, book hotels, and manage own profile
    HOTEL_MANAGER   // Admin-level user who can manage hotels, rooms, and inventory
}
